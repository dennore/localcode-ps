---
name: screenshot
description: Capture a screenshot of the primary monitor with cursor overlay.
---

# Screenshot

Captures the entire primary monitor with cursor position overlay.

```powershell
.\.agents\skills\screenshot.ps1
```

Saves `screenshot.png` in project root. Use `read` tool on it to view.

Use before click/sendkeys actions to identify coordinates.
